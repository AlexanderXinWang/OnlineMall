数据集三个属性的含义及类型：
含义：分别是用户id，商品id，用户对该商品的评分
类型：分别是integer integer Double(大写开头)

数据集的格式
属性与属性之间用\t（制表符，虽然看着像空格）隔开

test目录下的是测试本地环境用的代码

这些代码还是单独建一个spark项目运行测试吧，注释我也写的很详细了
测试的数据放在根目录下，和src文件同级，不要放在src里面

然后Rates这个类我也放在test目录下了

注意修改    .setMaster("-yarn");  //这个如果要在hdfs中运行,改成-yarn
然后数据集的例子我也放在test目录下了！！！！


不要把这些代码集成到咱们的项目里
咱们的项目只需要查询咱们的数据库就行了
我们在服务器上训练模型，在服务器上获得推荐的数据，保存在咱们的本地数据库中，这样查询会很快
总之三部：
    第一步：createModel.java：在服务器上运行，产生并保存模型
    第二步：createResult.java：在服务器上运行，产生并保存用户的推荐数据
    第三步：saveToJDBC.java:在我们的自己电脑上运行，把服务器上的用户的推荐数据保存在自己的电脑上

    第一步和第二步的代码其实可以放在一起