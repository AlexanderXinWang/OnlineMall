<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xju.onlinemall.mapper.SystemLogMapper">
    <select id="selectByMyExample" parameterType="com.xju.onlinemall.common.domain.SystemLogExample" resultMap="BaseResultMap">
        select
        <if test="distinct">
            distinct
        </if>
        <include refid="Base_Column_List" />
        from tb_system_log
        <if test="_parameter != null">
            <include refid="Example_Where_Clause" />
        </if>
        order by create_time desc
    </select>
    <select id="selectSystemLogOrderByCreateTime" resultMap="BaseResultMap">
        select * from tb_system_log order by create_time desc
    </select>
</mapper>